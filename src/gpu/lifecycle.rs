use super::*;
use super::util::{upload_slice, upload_rgba, make_depth};

impl Renderer {
    pub fn new(context: &gpu::Context, screen: gpu::Extent, format: gpu::TextureFormat) -> Self {
        todo!("see artifacts/gpu_module/lifecycle.rs — full body verified locally")
    }
}
